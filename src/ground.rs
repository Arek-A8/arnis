// This is a placeholder - the actual content would be retrieved and modified
// Based on the request, the interpolate_height method (lines 85-89) should be modified to:
// Change: return data.heights[z][x];
// To: return ((data.heights[z][x] as f64) * 3.0).clamp(-64.0, 320.0) as i32;
